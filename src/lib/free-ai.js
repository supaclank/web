// OpenCode serves a credential-free default model. The model name is
// disclosure copy only; preview sessions follow OpenCode's current default.
export const FREE_AI_CHOICE = Object.freeze({
  provider_id: 'opencode',
  backend: 'opencode',
  display_name: 'OpenCode',
  current_model_name: 'Big Pickle'
});
