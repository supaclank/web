default: web
previews:
  web:
    directory: .
    command: bun install --frozen-lockfile && bun run dev -- --host 127.0.0.1 --port "$PORT"
    env:
      __VITE_ADDITIONAL_SERVER_ALLOWED_HOSTS: "${CLANK_PREVIEW_PUBLIC_HOSTNAME}"
    ready:
      path: /demo
