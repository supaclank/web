# Clank builder

Mode: Persuade on entry; Operate on the board.

Direction: the user's dark canvas, explicitly monochrome and minimal. Graphite, gray, and white; no pink. Clank presence retains its approved local blue, including the cursor, avatar background, @Clank header button, and chat caret. The user also approved green for demo diff additions/current work, red for diff removals, and muted orange on selected node borders. Ordinary controls retain the monochrome palette; Idle remains gray. Preserve the mascot silhouette in monochrome. The prompt is the visual center. Cloud/local execution choices belong inside the prompt on the landing page. New/Import chooses the project source: New reveals web/mobile starter choices, while Import reveals a repository URL field. Explore integrated source controls, an attached source panel or tab, and one separate source node with a short dotted connection. The need for any edge remains open; avoid decorative arrows and extra nodes. Any connection used is dotted. Keep typography, controls and internal spacing compact, terse and sharp; avoid excessive padding. The plus remains an attachment affordance; explore dropping material directly on the prompt instead of a generic add-anything node. A fading dot field may animate slowly and stops for reduced motion.

The prompt grows into a live project widget on a pannable Svelte Flow board without a page navigation. Each widget contains agent conversation and app preview. Node headers are drag handles; forms, preview iframes and transcripts preserve normal interaction. Narrow screens keep readable widgets with chat/preview tabs and allow panning between projects. Web previews render in the board. Expo previews require the Clank phone app; show its download while the preview starts and a QR code when ready. No browser approximation or streamed simulator for Expo.

Quality bar: working input and output selections, explicit active paths, no fake controls, readable 13–16px operational type, keyboard focus, deliberate empty/loading/error states, private prompts excluded from recording. Browser previews use isolated origins and sandboxed iframes. Board positions persist per account; session contents come from the host.

Workflow: code-first remains the normal preference. For this iteration, the user explicitly requested image mockups before further implementation. The three compositions under `.impeccable/mocks/minimal-landing/` were rejected. Four follow-up layouts under `.impeccable/mocks/new-import-four/` were also rejected as too cluttered. The user accepted the general combination of 01 Ports and 07 Project tab in `.impeccable/mocks/ports-project-tab/01-ports-project-tab.png`: a bare composer with the small project tab and a closed Cloud menu in its footer. The right-hand output node remains an open design question. New/Import belongs in the project-tab menu; show only current choices on the resting surface. Avoid expanded setup forms, selector rails and nested textarea borders. Ignore content below the prompt for this round. Reusable repository nodes with multiple attached prompts remain a future idea, outside this iteration.

Accepted import direction: the repository URL is an input node on the left, as in `import-three/03-repository-node.png`. Added, pasted and dropped images become left input nodes too. Import does not guess an app type. Keep execution limited to one repository and use existing image attachments. Resume code-first implementation using the comps as visual references.

Future canvas direction: the code node represents a repository's working copy and diff, with PR status/action associated with it. Previews are separate outgoing nodes connected by dotted edges. One working copy can have multiple previews, such as web and mobile targets in a monorepo; each preview can have its own running state and share link. Multiple previews are the reason for this separation, independent of supporting multiple repository inputs. The agent must be able to inspect previews and use the results to continue editing; whether that feedback needs a visible edge remains open. Feeding a preview into another session and representing backend services as preview nodes are future possibilities, not current implementation requests. Backend preview YAML support is deferred. This direction does not change the current combined chat/preview widget yet. No generic artifacts feature, multi-repository schema, automation or voice implementation in this pass.

## Direction contract

THESIS: Build apps and keep your code. A compact real prompt leads directly into a framed, interactive example of the canvas workspace.

OWN-WORLD: Preserve graphite surfaces, gray borders, white controls and the monochrome mascot. Small project tab, bare textarea, precise ports and a plain browser frame; terse startup-style copy and spacing.

STORY: Understand web/mobile and local/cloud immediately; start from a prompt or inspect the labelled demo as an agent edits code and its preview starts. The demo is illustrative and never provisions a host.

FIRST VIEWPORT: Compact navigation, a direct headline and two lines of explanation above the real composer, followed by a framed Svelte Flow demo. Preserve supporting product facts below. Repository edges meet the project-tab header; image edges meet the lower-left plus; outgoing edges begin at the lower-right submit button. Dotted motion travels source to destination. Mobile keeps usable type and lets the demo be panned rather than shrinking its content to illegibility.

FORM: User-pinned combination of Ports 01 and Project tab 07, with repository-node import; assigned direction seed cf551c2b.

MOTION: Initial playback and Replay animate a small labelled cursor through prompt typing → submit → code changes → preview startup → checkbox click, ending at three of four habits complete. Pause, offscreen visibility, and a hidden document freeze the scripted clock. Reduced motion offers Next step with a static clickable cursor and no automatic checkbox click. User-created sessions progress after manual submission without scripted cursor actions and begin at two of four habits complete. The clickable cursor persists after completion and manual interaction. Active outgoing connections carry a travelling pulse from the submit port; idle dots move in the same direction. Use CSS and the existing Svelte Flow dependency, no new animation runtime. Preview/backend infrastructure is outside this demo pass.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance

Finish completed for the repository/image input pass: fresh visual review required no visual changes; documentation fix scored resolved with a ship verdict. DESIGN.md and design.json describe the shipped monochrome controls. No new runtime raster assets. Validation: 103 tests pass, two live-service tests skipped, production build passes; picker/paste and unsigned draft handoff/reload checked on desktop/mobile. Real host execution remains unverified. Review evidence lives under `.impeccable/review/`.

Next iteration: preserve substantially more of the previous landing page's content and structure around the prompt. Treat the current page as a first iteration, not the approved final composition; decide which previous sections to retain with the user.

Canvas session menu extension: inherit the current Operate styling. Right-click empty canvas to open a compact, one-action New session menu. On the real board, place a fresh draft at the clicked flow coordinate without moving the viewport or reusing another unsent draft. Clamp the menu to screen bounds, support Shift+F10 and Escape, and retain native context menus in node content. In the landing demo, create an independent editable composer at that flow coordinate without moving the camera; after node measurement, focus its textarea with scrolling prevented. Retain its custom prompt; manual submission adds local working-copy and preview nodes. Every prompt runs the same habit tracker with no authentication or backend work. Replay preserves other sessions and resets the original scripted example, hiding its outputs until their stages recur. Keep the existing minimal graphite styling with the approved Clank blue and semantic selection/diff/work colors. The textarea remains 14px with a 2px radius and visible focus outline. The cursor uses a 25px triangular pointer without a conventional tail, blue (#8aa4ff), dark tag text (#111b3c), pale edge (#dce4ff), and matching click pulse. Its Clank tag uses 11px text at weight 600, 3px by 7px padding, and a 4px radius.

Demo prompt and cursor pass: reviewed viewport evidence is under `.impeccable/review/demo-prompt/`. Validation reported 116 passing tests, two live-service skips, a passing production build, and zero detector antipatterns. No visual identity change or new shipping raster assets.

Colored cursor refinement: the user explicitly approved this localized collaborative-canvas treatment. Playback and the rest of the interface are unchanged. New captures are `.impeccable/review/colored-cursor/current.png`, `mobile.png`, and `desktop.png`; production build passes. No new shipping raster assets.

Clank companion: a circle-only blue mascot launcher in the upper-right corner and a persistent clickable Clank cursor open the selected right sidebar. Desktop chat is 312px wide beside the 414px-high canvas. At 699px and below, chat is 340px high below the 370px-high canvas, with no overlap. Canvas widths below 1110px focus the agent at 0.92 zoom. The chat header has a compact blue @Clank button and shows gray Idle or green Working text and dot for scripted playback and user requests. Clicking @Clank centers the cursor at the current zoom and enables follow, keeping chat open and playback intact. User messages are right-aligned graphite bubbles with no visible “you” label; assistant messages retain “clank”, with no header or message avatars. The inset composer starts a separate session, working-copy, and preview row for each request without moving the camera. Empty requests cannot be sent; Enter sends, Shift+Enter adds a line, and Close or Escape returns focus to the launcher. Chat and request status survive closing and reopening. Show session and View preview keep the sidebar open while focusing the selected canvas view. Requests run the fixed habit tracker without scripted cursor actions. Real AI orchestration and voice remain future direction; this demo has no authentication or backend integration. Ordinary controls remain monochrome; Clank presence uses blue, additions/current work use green, removals use red, and selected node borders use muted orange. No new raster assets.

Clank companion typography: 13px messages and desktop input with a 20px line height, 12px follow-up actions, and 16px mobile input with a 22px line height; compact metadata remains 10–11px. Earlier floating-chat confirmation captures are `.impeccable/review/clank-avatar/mobile-final.png` and `desktop-final.png`.

Earlier floating-chat validation: 117 tests pass, two live-service tests skip, and production build passes without new Svelte warnings. Viewport captures are `.impeccable/review/clank-avatar/current-open.png`, `mobile-working.png`, and `desktop-ready.png`.

Replay reliability fix: Replay now restarts the animation clock after a completed run. The real-browser regression in `scripts/browser-checks/demo-replay.js` failed before the fix and passed afterward; 117 tests pass, two live-service tests skip, and production build passes.

Selected presence direction: the user chose a right sidebar using the overlay chat styling reference, with right-aligned user bubbles, a “clank” assistant label, and no message avatars. This choice is implemented in the live demo. The A/B/C options under `.impeccable/mocks/clank-presence/`—Beside the cursor, Below the cursor, and History on the side—remain exploratory references superseded by this selection. The local HTML mock preview was blocked by browser policy and was not served, rendered, or opened through another route.

Shared conversation follow-up: [issue #275](https://github.com/supaclank/clank/issues/275) tracks a shared conversation component sourced in clank, consumed through a pinned version in web, with preview, web, and demo adapters. No extraction is included in this sidebar change.

Sidebar validation: 118 tests pass, two live-service tests skip, and production build passes without warnings. The detector reported no antipatterns, and visual review requested no material fixes.

Clank follow mode: opening either the avatar or cursor tag enables follow and opens the sidebar without interrupting scripted playback. Follow centers the cursor’s world position in the actual canvas, excluding the sidebar, while preserving zoom. It tracks through preview readiness, idle, and resize; Replay preserves enabled following. The Following Clank button exits follow, as do user panning, node dragging, explicit focus actions, and new user work. Show session and View preview continue to keep chat open. No new dependencies or raster assets.

Follow-mode validation: `scripts/browser-checks/demo-follow.js` failed before the fix and passes afterward, covering an active cursor click without interruption, centered follow through preview readiness and idle, and explicit exit. 120 tests pass, two live-service tests skip, and production build passes.

Worktree gestures: the approved demo graph is repository → session → worktree → preview. Drag the repository’s right port to blank canvas for a draft based on main; drag a Code node’s bottom Fork port for a draft based on that worktree, creating a child worktree by default. Connect a draft session’s output to an existing worktree’s left input to reuse it, or drag that input to blank canvas for a reuse draft. New/fork submission adds a separate worktree and preview; reuse updates the existing pair without duplicates. Only draft connections can change. Worktree lineage is retained, and one active demo writer is allowed per worktree. Busy fork/reuse attempts retain the draft with an error. Enter on repository, Fork, and worktree-input ports creates the corresponding draft. Creation from blank canvas or ports keeps the camera still; new prompt focus waits for node measurements and prevents scrolling. The @Clank header button retains the approved compact blue highlight. This adds no assets, dependencies, or backend features.

Worktree gesture verification: new coverage lives in `worktrees.test.js` and `connections.test.js`. Native desktop interactions checked repository, fork, reuse, and reconnect flows; `demo-follow.js` also passes. Release hit testing uses the actual release coordinates with `document.elementFromPoint`; touch coverage is a coordinate unit test, not a native touch-browser run. Review captures are `.impeccable/review/worktree-gestures/desktop.png` and `mobile.png`, taken as section captures at 1280×720 and 390×844 viewports.

Worktree gesture finish: 128 tests pass, two live-service tests skip, and production build and diff checks pass. Native repository dragging passes after the release-hit-test fix. Finish review marked that finding resolved and returned ship.

Demo node actions: selection uses muted orange (#d99a62) on each node’s actual borders, including the session tab and body, with no detached outline or glow. Selection and session-chat opening leave the camera unchanged; selection exits follow without stopping playback. Submitted session bodies open their corresponding conversation, and context labels remain draggable. Deletion UI is paused: Backspace/Delete does not remove nodes or open a dialog. Editable fields keep normal keyboard behavior. Creation from blank canvas, ports, or sidebar requests keeps the camera still; measured draft textareas focus with scrolling prevented. Explicit Reset view, View preview, and Clank follow remain camera actions.


Semantic colors and profile follow: the approved demo tokens are additions/Working green (#7fcf9b) and removals red (#f08b8b). Code examples show +42/−8, +28/−3, and +19/−5 with signs retained; sidebar Working colors both its label and filled dot, while Idle stays gray. The @Clank highlight is a native button named “Follow Clank on canvas”, with hover and keyboard focus, reusing follow to center the cursor without changing zoom, closing chat, or interrupting playback. The introduction now reads “Tell me what you have in mind. We’ll take it from there.” to leave room for future canvas assistance without claiming unimplemented abilities. The current demo still runs the fixed habit-tracker example. No runtime raster assets, dependencies, or backend changes. Capture directory: `.impeccable/review/semantic-colors/`.

Profile-follow validation: the browser regression fails before the fix and passes afterward, including clicking @Clank after Reset view to re-enable follow and recenter the cursor. The latest full run passed 132 tests with two live-service skips and a passing production build, before the final introductory-copy edit.

Session progress: the scripted sequence reads the project, edits HabitList.svelte, WeekView.svelte, and habits.ts, runs checks, starts the preview, and completes. File rows and signed diff counts grow incrementally; session/code heights and sidebar opening animate with reduced-motion guards. Code is visible after submission; preview first appears at startup and remains visible for same-worktree follow-ups. Each session has its own sidebar history and a follow-up composer enabled when work is complete. Grouping, backend features, and artifacts are deferred. No runtime assets or dependencies were added.

Progress options: development builds expose `?design=session-progress` with Prompt + activity, Activity first, Session card, and Mini chat. Prompt + activity is the current default; the compact Session card is the recommendation, not a user-approved final choice. The four options remain selectable exploration.

Current progress validation: 135 tests pass, two live-service tests skip, and production build and diff checks pass. Browser checks cover selection, paused deletion, height changes, session chat and continuation, output visibility, and context dragging. Evidence is `.impeccable/review/session-progress/01-prompt.png`, `02-activity.png`, `03-session.png`, `04-chat.png`, `desktop.png`, and `mobile.png`. The detector retains existing compact palette/type advisories and flags the intentional height/sidebar transitions; this pass does not change unrelated rules to suppress them. Finish review returned ship after scoring the documentation correction resolved; application visuals were unchanged.
